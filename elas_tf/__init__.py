"""
ElasTF: Elastic Distributed Training with TensorFlow.

This package provides:
- Baseline MultiWorkerMirroredStrategy training (`training.py`)
- Controller and worker processes for elastic training (`controller.py`, `worker.py`)
- Heartbeat utilities for worker liveness (`heartbeat.py`)
- Checkpoint helpers (`checkpointing.py`)
"""

